import { TextProps } from '@ui-kitten/components';
import { COLORS_MAP } from '@shared/config/colors';

export const secondaryTextProps: Partial<TextProps> = {
  style: { color: COLORS_MAP['color-basic-400'] },
  category: 'p1',
};
